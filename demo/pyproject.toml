[project]
name = "demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agent-transparency[kafka,ui]",
]

[tool.uv.sources]
agent-transparency = { path = "../" }
